/* tslint:disable:no-unused-variable */

import { TestBed, async } from '@angular/core/testing';
import { VatPipe } from './vat.pipe';

describe('Pipe: Vate', () => {
  it('create an instance', () => {
    let pipe = new VatPipe();
    expect(pipe).toBeTruthy();
  });
});
